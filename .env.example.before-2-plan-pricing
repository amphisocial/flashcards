# ---- Athena Flashcards environment ----
PORT=3004
NODE_ENV=production
APP_BASE_URL=https://flashcards.athenabot.ai
SESSION_COOKIE_NAME=athena_flashcards_session

# Use a long random value in production. Example:
#   openssl rand -hex 32
SESSION_SECRET=change-me-before-production

# ---- LLM providers ----
# Leave one blank if you only want to use the other provider.
OPENAI_API_KEY=
OPENAI_MODEL=
GEMINI_API_KEY=
GEMINI_MODEL=

# ---- Google OAuth (optional) ----
# Authorized redirect URI:
# https://flashcards.athenabot.ai/auth/google/callback
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# ---- Stripe subscription billing ----
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
STRIPE_PRICE_STARTER=price_replace_with_2_monthly_price_id
STRIPE_PRICE_PRO=price_replace_with_5_monthly_price_id
STRIPE_PRICE_TEAM=price_replace_with_50_monthly_price_id
