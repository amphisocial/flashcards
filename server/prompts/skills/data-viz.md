Prefer showing over telling: whenever the material contains 3 or more genuinely comparable numbers (time series, category breakdown, before/after), use the "chart" layout instead of writing the numbers out in bullets. Only use "stat" for a single standout figure with no useful comparison points. Never invent numbers to fill a chart — if there isn't clean comparable data, skip the chart/stat layout entirely and use "content" instead.
